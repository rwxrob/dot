#!/bin/bash

declare suf=""
declare what=$1

if [[ -z "$what" ]]; then
  usageln 'open <what>'
  exit 1
fi

if [[ ! -r "$what" ]]; then
  if [[ ! "$what" =~ ^http ]]; then
    what=https://$what
  fi
fi

exec xdg-open "$what"



